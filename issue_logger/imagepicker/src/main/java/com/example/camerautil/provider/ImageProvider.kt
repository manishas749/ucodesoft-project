package com.example.camerautil.provider

/***
 *
 * ImageProvider enum class
 *      * Image provider for selection of where to get image
 *
 ***/
enum class ImageProvider {
    GALLERY, CAMERA, BOTH
}