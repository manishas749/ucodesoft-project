<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Cinemago" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">#878FBC</item>

        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Splash" parent="Theme.SplashScreen">
        <item name="android:windowActionBar">false</item>
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.Cinemago</item>
    </style>

    <style name="Base.Theme.Cinemago.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Cinemago" parent="Base.Theme.Cinemago" />
</resources>